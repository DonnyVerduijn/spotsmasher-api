const model = require("./Media.model");
const resolvers = require("./Media.resolvers");
const queries = require("./Media.queries");
const test = require("./Media.spec");

module.exports = {
  model,
  resolvers,
  queries,
  test
};
